Для начала выведем Process ID текущей сессии интерпретатора командной строки

Для этого выполним команду

`echo $$`{{execute}}

Эта информация нам понадобится, чтобы определить в какой сессии мы находимся