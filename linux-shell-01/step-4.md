Теперь откроем интерпретатор `sh`:

`sh`{{execute}}

Как видите, приглашение командной строки поменялось

Стало более лаконичным

Узнаем Process ID новой сессии:

`echo $$`{{execute}}

PID снова отличаются