Теперь проверим, что `my-app` можно запустить из любого каталога

## Детали

Запустите `my-app`:

`my-app`{{open}}

Работает

Перейдите в домашний каталог:

`cd ~`{{open}}

Повторите команду:

`my-app`{{open}}

Тоже работает